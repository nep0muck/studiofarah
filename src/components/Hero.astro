---
interface Props {
  id?: string;
  whatsappNumber?: string; // im Format 491711234567
  whatsappMessage?: string;
}

const {
  id = "hero",
  whatsappNumber = "491711234567", // TODO: deine echte Nummer einsetzen
  whatsappMessage = "Hallo, ich interessiere mich für eine Behandlung und würde gerne einen Termin vereinbaren."
} = Astro.props;

const whatsappHref = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(
  whatsappMessage
)}`;
---

<section id={id} class="relative overflow-hidden bg-bg text-text antialiased">
  <!-- Hintergrund-Gradient -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary-soft/30 via-bg to-primary/20 pointer-events-none"></div>

  <div class="relative mx-auto flex min-h-[70vh] max-w-6xl flex-col gap-10 px-6 py-16 md:flex-row md:items-center md:py-24">
    <!-- Textbereich -->
    <div class="flex-1 space-y-6">
      <p class="inline-flex items-center rounded-full bg-surface/80 px-4 py-1 text-xs font-medium uppercase tracking-[0.2em] text-primary-deep shadow-sm ring-1 ring-border/60">
        Kosmetik &amp; Wellness • Nur für dich
      </p>

      <h1 class="text-4xl font-semibold leading-tight tracking-tight text-primary-deep sm:text-5xl lg:text-6xl">
        Deine Auszeit für<br class="hidden sm:block" />
        <span class="bg-gradient-to-r from-primary-deep via-primary to-primary-soft bg-clip-text text-transparent">
          strahlende Haut &amp; innere Ruhe
        </span>
      </h1>

      <p class="max-w-xl text-base leading-relaxed text-text-soft">
        Gönn dir eine Pause vom Alltag: individuell abgestimmte
        Gesichtsbehandlungen, wohltuende Massagen und Beauty-Rituale,
        die Körper und Seele ins Gleichgewicht bringen.
      </p>

      <!-- Bullets -->
      <div class="grid gap-3 text-sm text-text-soft sm:grid-cols-2">
        <div class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary-soft/30 text-[10px] font-semibold text-primary-deep">
            ✓
          </span>
          <span>Sanfte, hautverträgliche Produkte</span>
        </div>
        <div class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary-soft/30 text-[10px] font-semibold text-primary-deep">
            ✓
          </span>
          <span>Persönliche und diskrete Atmosphäre</span>
        </div>
        <div class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary-soft/30 text-[10px] font-semibold text-primary-deep">
            ✓
          </span>
          <span>Wellness-Behandlungen für Gesicht &amp; Körper</span>
        </div>
        <div class="flex items-start gap-2">
          <span class="mt-1 inline-flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-primary-soft/30 text-[10px] font-semibold text-primary-deep">
            ✓
          </span>
          <span>Flexible Termine nach Vereinbarung</span>
        </div>
      </div>

      <!-- Buttons -->
      <div class="flex flex-wrap items-center gap-4 pt-4">
        <!-- WhatsApp Business CTA -->
        <a
          href={whatsappHref}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center justify-center gap-2 rounded-full bg-primary-deep px-8 py-3 text-sm font-medium text-bg shadow-md shadow-primary-deep/25 transition hover:-translate-y-0.5 hover:bg-primary"
        >
          <span>Termin via WhatsApp</span>
          <span class="text-xs uppercase tracking-[0.16em] text-bg/80">
            Schnell &amp; direkt
          </span>
        </a>

        <!-- Zweiter CTA (Leistungen) -->
        <a
          href="#leistungen"
          class="inline-flex items-center justify-center rounded-full border border-border bg-surface/80 px-7 py-3 text-sm font-medium text-primary-deep shadow-sm transition hover:bg-bg-soft"
        >
          Behandlungen entdecken
        </a>
      </div>

      <!-- Kleiner Zusatz -->
      <p class="pt-2 text-xs text-text-soft/80">
        ✨ Schreibe uns einfach per WhatsApp – wir melden uns schnellstmöglich mit Terminvorschlägen.
      </p>
    </div>

    <!-- Bild / Placeholder-Bereich -->
    <div class="flex-1">
      <div class="mx-auto max-w-md rounded-3xl bg-surface/90 p-4 shadow-xl shadow-primary-soft/30 ring-1 ring-border/60">
        <div class="aspect-[4/5] overflow-hidden rounded-2xl bg-gradient-to-b from-primary-soft/60 via-bg to-bg-soft">
          <div class="flex h-full flex-col items-center justify-center gap-3 px-6 text-center text-sm text-text-soft">
            <div class="rounded-full bg-surface/80 px-3 py-1 text-[11px] font-medium uppercase tracking-[0.18em] text-primary-deep">
              Platzhalter
            </div>
            <p class="text-base font-medium text-primary-deep">
              Atmosphärisches Studiofoto
            </p>
            <p class="text-xs leading-relaxed">
              Ersetze diesen Bereich mit einem hochwertigen Foto deines Studios,
              deiner Behandlungsliege oder Beauty-Produkte – gerne in sanften Rosa- und Lilatönen.
            </p>
          </div>
        </div>

        <div class="-mt-8 ml-auto w-40 rounded-2xl bg-primary-deep px-4 py-3 text-xs text-bg shadow-lg shadow-primary-deep/40">
          <p class="font-semibold">Heute noch entspannen</p>
          <p class="text-[11px] text-bg/80">
            Freie Termine verfügbar – schreib uns einfach per WhatsApp.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

